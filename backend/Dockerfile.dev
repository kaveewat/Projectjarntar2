FROM node:20-alpine

WORKDIR /app

# Install dependencies first (layer cache)
COPY package*.json ./
RUN npm install

# Source code is mounted via bind mount at runtime
# This COPY is only for the initial image build context check
COPY . .

EXPOSE 5001

# nodemon for hot-reload in development
CMD ["npx", "nodemon", "src/index.js"]
