FROM node:20-alpine

WORKDIR /app

# Install dependencies first (layer cache)
COPY package*.json ./
RUN npm install

# Source code is mounted via bind mount at runtime
COPY . .

EXPOSE 5173

# Vite dev server — bind to 0.0.0.0 so Docker can forward the port
CMD ["npx", "vite", "--host", "0.0.0.0"]
